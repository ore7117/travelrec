/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Scans all components in src folder
    './public/index.html', // Include HTML files if necessary
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
